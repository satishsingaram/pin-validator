pipeline {
    agent any
    tools {
            gradle 'myGradle'
    }

    triggers {
        pollSCM('*/5 * * * *')
    }

    stages {
        stage('Compile') {
            steps { //Checking out the repo
               checkout
            }
            steps {
                sh 'gradle build'
            }
        }
        stage('Unit Tests') {
            steps {
                sh 'gradle classes'
            }
            post {
                always {
                    junit '**/build/test-results/test/TEST-*.xml'
                }
            }
        }
    }
    post {
        failure {
            echo 'build failed'
        }
    }



}